#include "main.h"

/**
 * _abs - return the absolute valor of n
 * @n: character to be operated
 *
 * Return: The absolute valor
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
	}
}
