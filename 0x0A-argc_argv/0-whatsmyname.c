#include <stdio.h>

/**
 * main - Print the name of this progam
 * @argc: Length of argv
 * @argv: Array of size argc
 *
 * Return: 0
 */
int main(__attribute__((unused)) int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
